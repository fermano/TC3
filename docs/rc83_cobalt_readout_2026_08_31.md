# Cobalt RC83 readout note

The evening handoff has two partly conflicting observations: the batch count is green, but the known midnight slot fixture was last sampled from a staging replay, not the final release branch artifact.

The mainline alias branch may contain the useful behavior, but it writes a destination-shaped row and predates the market row migration.
